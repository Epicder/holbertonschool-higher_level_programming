#!/usr/bin/python3

def add_tuple(tuple_a=(), tuple_b=()):
      a = tuple_a[:2] + (0,) * (2 - len(tuple_a))
      b = tuple_b[:2] + (0,) * (2 - len(tuple_b))
      
      tuple_result = (a[0] + b[0], a[1] + b[1])
      return tuple_result
      
